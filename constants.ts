
import { Unit, MaterialUnit } from './types';

export const CURRENCY = 'KGS';

export const PRODUCT_UNITS: Unit[] = ['slice', 'whole', 'piece'];

export const MATERIAL_UNITS: MaterialUnit[] = ['kg', 'g', 'L', 'ml', 'piece'];
